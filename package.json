{
  "name": "kolek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kolek — SaaS de gestion pour banquiers ambulants",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "db:start": "npx supabase start",
    "db:stop": "npx supabase stop",
    "db:reset": "npx supabase db reset",
    "db:env": "node scripts/env-test.mjs",
    "test:db": "npm run db:env && vitest run --config supabase/tests/vitest.config.ts",
    "test:scripts": "vitest run scripts",
    "verifier:bundles": "node scripts/verifier-bundles.mjs",
    "verifier:j1": "npm run db:reset && npm test && npm run test:scripts && npm run test:db && npm run build && npm run verifier:bundles"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "dotenv": "^17.4.2",
    "vitest": "^4.1.10"
  }
}
