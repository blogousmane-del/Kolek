# Le site public est la surface commerciale : il a vocation à être indexé.
# C'est le seul des trois. L'autorisation est écrite explicitement plutôt que
# laissée à l'absence de fichier — sans ce fichier, la réécriture `/*` répond à
# `/robots.txt` par du HTML, et l'intention ne se lit nulle part.
User-agent: *
Allow: /

# Le sitemap se déclare ici en plus d'être soumis dans Search Console : un robot
# qui n'est pas Google ne lit que ce fichier, et l'adresse doit être absolue —
# un chemin relatif est ignoré par la spécification.
Sitemap: https://kolek-site.netlify.app/sitemap.xml
