import { PageLegale } from './PageLegale';

/**
 * Les conditions générales — `/conditions`.
 *
 * Squelette de la tâche 1 : la tâche 2 rédige le contenu (objet, paliers,
 * paiement, résiliation, obligations du collecteur, droit applicable). Vide
 * pour l’instant, pour que `App.tsx` compile et route vers la page.
 */
export function Conditions() {
  return <PageLegale titre="Conditions générales" miseAJour="2026-09-18" />;
}
