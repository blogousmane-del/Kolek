import { PageLegale } from './PageLegale';

/**
 * La politique de confidentialité — `/confidentialite`.
 *
 * Squelette de la tâche 1 : la tâche 3 rédige le contenu (plan de l’article 9,
 * tableau des durées, sous-traitants, droits et procédure d’exercice). Vide
 * pour l’instant, pour que `App.tsx` compile et route vers la page.
 */
export function Confidentialite() {
  return <PageLegale titre="Politique de confidentialité" miseAJour="2026-09-18" />;
}
